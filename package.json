{
    "name": "ezviz-rag-agent-monorepo",
    "private": true,
    "scripts": {
        "api:dev": "pnpm -r api:dev",
        "api:build": "pnpm -r api:build",
        "typecheck": "pnpm -r typecheck",
        "mcp:dev": "pnpm -r mcp:dev",
        "mcp:build": "pnpm -r mcp:build"
    },
    "devDependencies": {
        "typescript": "^5.7.3"
    }
}